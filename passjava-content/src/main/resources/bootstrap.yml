server:
  port: 9000

spring:
  application:
    name: passjava-content

  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:1111
      config:
        server-addr: 127.0.0.1:1111
        file-extension: yaml
